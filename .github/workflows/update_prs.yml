name: Update Profile README

on:
  schedule:
    - cron: '0 0 * * *' # Runs daily at midnight
  workflow_dispatch: # Allows manual trigger

permissions:
  contents: write

jobs:
  update-readme:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Fetch Recent Pull Requests
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          prs=$(curl -s -H "Authorization: token $GITHUB_TOKEN" \
          "https://api.github.com/search/issues?q=is:pr+author:StancuFlorin&sort=created&order=desc&per_page=5")

          echo "## Recent Open Source Contributions ✨" > pr_list.md
          echo "" >> pr_list.md
          
          # Filter PRs to exclude those from the 'StancuFlorin' organization
          echo "$prs" | jq -r '.items[] | select(.repository_url | contains("github.com/StancuFlorin") | not) | "- [\(.title)](\(.html_url)) - \(.repository_url | split("/") | last) :octocat:"' >> pr_list.md

      - name: Update README
        run: |
          awk '/<!-- START PRS -->/{print; system("cat pr_list.md"); next} /<!-- END PRS -->/{found=1} {if(found) print}' README.md > README_tmp.md
          mv README_tmp.md README.md

      - name: Commit and Push Changes
        run: |
          git config --local user.email "your_email@example.com"
          git config --local user.name "GitHub Action"
          git add README.md
          git commit -m "Update README with recent pull requests" || echo "No changes to commit"
          git push
